/**
 * 秦岭碳收支遥感监测系统 - 小程序入口文件
 * 
 * 功能描述：
 * 1. 应用初始化：配置云开发环境，检查配置
 * 2. 全局数据管理：提供全局数据存储和访问
 * 3. 生命周期管理：处理应用启动、显示、隐藏等事件
 * 
 * 主要配置：
 * - 云开发环境ID: cloudbase-8gdof83j7fdc6094
 * - 权限配置：地理位置权限
 * - 全局样式：通过app.wxss定义
 * 
 * 修改历史：
 * - 2026-03-03: 为代码添加详细注释
 * - 2026-03-03: 优化云开发初始化逻辑
 * 
 * 注意事项：
 * - 云开发环境需在微信开发者工具中开通
 * - 地理位置权限需要用户授权
 * - 全局数据可在所有页面中通过getApp()访问
 */
// app.js
App({
  onLaunch() {
    const { checkConfig } = require('./config.js');
    checkConfig();
    
    // ============================================
    // 微信云开发配置说明（毕业设计推荐方案）
    // ============================================
    // 1. 在微信开发者工具中点击"云开发"按钮开通云开发
    // 2. 创建环境并获取环境ID（免费额度足够毕业设计使用）
    // 3. 用真实环境ID替换下面的'your-env-id'
    // 4. 云开发提供：云存储（上传图片/JSON）、云数据库、云函数
    // 5. 无需自建服务器，无需备案，与小程序无缝集成
    // ============================================
    
    // 初始化云开发（已开通，环境ID: cloudbase-8gdof83j7fdc6094）
    try {
      wx.cloud.init({
        // 云开发环境ID（已开通）
        env: 'cloudbase-8gdof83j7fdc6094',
        traceUser: true
      });
      console.log('云开发初始化成功');
    } catch (error) {
      console.warn('云开发初始化失败', error);
      console.log('提示：云开发初始化失败不影响基础功能，但管理员上传功能将使用模拟上传');
    }
    
    // 全局配置
    this.globalData = {
      baseUrl: 'https://your-api.com',
      mapCenter: {
        latitude: 33.5108767,
        longitude: 108.2590920
      },
      version: '1.0.0'
    };
  }
});